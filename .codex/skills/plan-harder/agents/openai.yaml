interface:
  display_name: "Plan Harder"
  short_description: "Planning depth modifier, not a standalone default planner."
  default_prompt: "Use $plan-harder to complete this task using its defined workflow and constraints."
