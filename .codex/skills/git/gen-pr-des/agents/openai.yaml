interface:
  display_name: "Gen Pr Des"
  short_description: "- Generate a pull request description from git history and diff data between a base and head ref. Use when the user a..."
  default_prompt: "Use $gen-pr-des to complete this task using its defined workflow and constraints."
