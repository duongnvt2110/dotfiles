interface:
  display_name: "Figma Detail Extractor"
  short_description: "Specialized Figma extraction workflow for deep structured outputs (component specs, layer tables, asset lists, and."
  default_prompt: "Use Figma MCP to extract detailed component specs, layer tables, and assets from the provided Figma node(s)."

dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Figma MCP server"
      transport: "streamable_http"
      url: "https://mcp.figma.com/mcp"
